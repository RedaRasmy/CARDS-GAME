
export default function useCard() {
    // use redux : take the cards left (useSelector)
    // choose a random card 
    // filter it 
    // and update the state (useDispatch)
    // return the choosen card
}